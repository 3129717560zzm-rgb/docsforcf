---
title: 请求错误类问题
description: 403、429、model not found、超时这类请求错误怎么查。
---

# 请求错误类问题

报错先看操练场，再看调用日志。

### 错误速查

| 报错 | 常见原因 | 先做什么 |
| --- | --- | --- |
| `401` | Key 不可用 | 看 [认证与 API Key 类问题](/guide/troubleshooting/auth) |
| `403` | 模型或通道没权限 | 换分组，换模型 |
| `429` | 并发太高或请求太快 | 降低并发，增加重试间隔 |
| `model not found` | 模型名写错或无权限 | 去操练场复制模型名 |
| `502` / `504` / `524` | 上游超时或异常 | 短请求重试，换模型 |

### `model not found`

不要凭记忆手打模型名。

正确做法：

1. 打开 [操练场](https://zzmapi.zzmsgdsg.xyz/console/playground)
2. 选中目标模型
3. 直接复制模型 ID
4. 粘贴回工具

### `403`

一般查这两个：

- 当前 Key 所在分组能不能用这个模型
- 余额够不够

### `429`

常见场景：

- 多个工具共用一个 Key
- 批量脚本并发太高
- 短时间内连续重试太快

处理：

- 一个工具一个 Key
- 降低并发
- 增加重试等待

### `502` / `504` / `524`

这类更像上游超时或响应太慢，不一定是你本地配置错了。

先试：

- 换一个更短的请求
- 换模型
- 稍后重试

### 还是不确定

去群里时带上：

- 模型名
- 报错时间
- 截图

QQ群：`1098412601`
